# Ana review notes (F2 digester)

1. Thin bar locked in code at <40 human messages OR <10 distinct UTC days; 45 then 90, never unbounded. Confirm before client use.
2. Insufficient PDF includes the $99 / 2-week Stripe URL as plain text when forward-watch was not already purchased.
3. Optional next step names Command Desk Setup at $799 and AUDIT150 / $150 / 30 days from the one-pager. Soft CTA only.
4. Specialist seats are derived from cluster kinds (FAQ Drafter, Handoff Coordinator, Ticket Tracker, Status Digest, Approval Clerk) plus Escalation Owner / Knowledge Curator. Roles only, no prompts.
5. FAQ split uses English seed terms (refund, shipping, login, etc.) plus overlap clustering. Ranking is frequency x active days, so daily standup can outrank FAQ. Ana can reorder in the draft.
6. `--llm` only polishes titles/triggers when OPENAI_API_KEY is set. Fixture smoke ignores it.
7. Channel filter keeps named intake channels when they match the export. Unnamed extra files in slack/ are ignored in that case.
8. Confirm we should keep $799 Setup on the pack. Brief stressed AUDIT150; one-pager still lists Setup as a separate SKU.
